{
  "name": "test-workers",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:delete-cache": "cypress cache clear",
    "cypress:install": "npm install cypress --save",
    "cypress:open": "cypress open",
    "cypress:run": "cd scripts && npx cypress run"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.814.0",
    "axios": "^0.19.2",
    "cron": "^1.8.2",
    "file-system": "^2.2.2",
    "q": "^1.5.1",
    "shelljs": "^0.8.3"
  },
  "devDependencies": {
    "cypress": "^4.3.0"
  }
}
